# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Command line interface tests

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:
    inputs:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11"]

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip' # caching pip dependencies
        cache-dependency-path: '**/pyproject.toml'
    - name: Download fixtures
      run: |
        wget -h
        wget https://www.dropbox.com/scl/fi/ivsqbao9we1lnyyyh43cc/ci_data.zip?rlkey=b9lcqxs63iv2409q5a7q1oh9z&dl=0
        ls -la
        ls -la /
        pwd
        mv ci_data.zip?rlkey=b9lcqxs63iv2409q5a7q1oh9z ci_data.zip
        unzip ci_data.zip
        ls -la
        ls -la ci_data
        pwd
    - name: Run VAMB
      run: |
        vamb bin basic --outdir outdir_vamb --fasta ci_data/catalogue_mock.fna.gz --rpkm ci_data/abundance_mock.npz -l 32 -e 100 -q 25 75 -o C --minfasta 200000
    - name: Run TaxVAMB
      run: |
        vamb bin taxvamb --outdir outdir_taxvamb --fasta ci_data/catalogue_mock.fna.gz --rpkm ci_data/abundance_mock.npz --taxonomy ci_data/taxonomy_mock.tsv
        cat outdir_taxvamb/log.txt

