name: ptracker_pipeline

channels:
  - pytorch
  - conda-forge
  - bioconda
  - defaults

dependencies:
  - python==3.12
  - snakemake==8.26.0 
  - minimap2
  - samtools
  - spades
  - blast
  - snakemake
  - networkx
  - pytorch
  - pysam
  - pip
  - pip:
      - fastnode2vec
      - -e .. # Install the cli wrapper - paths are relative to the location of the .yaml file
      - -e ../.. # Install vamb
